<h1 class="text-xl font-bold text-center py-6">
  投稿したmy楽器
</h1>
<div class="flex flex-wrap justify-center gap-6">
  <% if @my_instruments.present? %>
    <%= render @my_instruments %>
  <% else %>
    <div class="mb-3">my楽器を投稿していません</div>
  <% end %>
</div>
<%= paginate @my_instruments %>
