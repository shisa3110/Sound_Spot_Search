<h1 class="text-xl font-bold text-center py-6">
  ブックマークした施設
</h1>

<div class="flex flex-wrap justify-center gap-6 mx-5 py-3">
  <% if @bookmark_spots.present? %>
    <%= render @bookmark_spots %>
  <% else %>
    <div class="text-center">
      <div class="mb-3">ブックマークした施設がありません</div>
    </div>
  <% end %>
</div>
<%= paginate @bookmark_spots %>